name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build and Release
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Build placeholder
        run: echo "Release build pipeline — to be implemented"
